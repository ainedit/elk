input {
    beats {
        port => 5044
    }
}
output {
    elasticsearch {
        #hosts => ["127.0.0.1:9200"]
        #data_stream => "true"
	index => "ejemplo-%{+YYYY.MM.dd}"
    }
    stdout {
        codec => json_lines
    }
}


